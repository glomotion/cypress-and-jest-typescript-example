{
  "name": "cyts",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test:jest": "jest",
    "test:cy:run": "cypress run-ct",
    "pretest": "npm run typecheck",
    "test": "npm run test:cy:run; npm run test:jest",
    "cy:open": "cypress open",
    "build": "tsc",
    "lint": "tsc --project tsconfig.lint.json",
    "typecheck": "tsc --noEmit",
    "lint:cypress": "tsc --project cypress/tsconfig.json"
  },
  "devDependencies": {
    "@cypress/react": "^5.3.3",
    "@cypress/webpack-dev-server": "^1.1.3",
    "@types/chai": "^4.1.7",
    "@types/jest": "^26.0.22",
    "@types/mocha": "^8.0.0",
    "cypress": "7.1.0",
    "jest": "^26.6.3",
    "local-cypress": "^1.2.1",
    "ts-jest": "26.5.4",
    "typescript": "^4.1.4"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
